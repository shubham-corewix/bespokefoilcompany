#!/usr/bin/env node
/*
 * BFC COMPONENT LIBRARY generator
 * --------------------------------
 * Reads:
 *   - scripts/component-registry.js  (the components, single source of truth)
 *   - _redirects                     (to auto-list currently published pages)
 * Emits:
 *   - component-library.html
 *
 * Run after editing the registry, or after publishing a new page:
 *     node scripts/generate-component-library.js
 *
 * The "Published pages" index at the top auto-updates from _redirects on every
 * run, so it always reflects what's actually live. (Manual-but-easy: you add a
 * component to the registry and rerun; the page index needs no touching at all.)
 */
const fs = require('fs');
const path = require('path');
const { CATEGORIES, COMPONENTS } = require('./component-registry.js');

const ROOT = path.join(__dirname, '..');
const OUT = path.join(ROOT, 'component-library.html');

/* ---- parse _redirects for the live-pages index (mirrors sitemap generator) ---- */
function readPublishedPages() {
  const rd = fs.readFileSync(path.join(ROOT, '_redirects'), 'utf8');
  const pages = [];
  for (const line of rd.split('\n')) {
    const t = line.trim();
    if (!t || t.startsWith('#')) continue;
    if (!/\s200\b/.test(t)) continue;
    const hashAt = t.indexOf('#');
    const meta = hashAt >= 0 ? t.slice(hashAt + 1).trim() : '';
    if (/^exclude/i.test(meta)) continue;            // internal/hidden routes
    const route = t.split(/\s+/)[0];
    const parts = meta.split('|').map(s => s.trim());
    const category = parts[0] || 'Page';
    const label = parts[1] || route;
    pages.push({ route, category, label });
  }
  return pages;
}

const esc = s => String(s).replace(/&(?![a-z#0-9]+;)/g, '&amp;');

function componentBlock(c) {
  const frameCls = c.dark ? 'lib-frame is-dark' : 'lib-frame';
  return `
<div class="lib-item" id="${c.code.toLowerCase()}">
  <div class="lib-item-bar">
    <span class="lib-id"><span class="lib-ref">${c.code}</span><h3>${esc(c.name)}</h3></span>
    <span class="lib-desc">${esc(c.desc)}</span>
  </div>
  <div class="${frameCls}">
    <div class="lib-preview">${c.html}
    </div>
  </div>
</div>`;
}

function categorySection(cat) {
  const items = COMPONENTS[cat.id] || [];
  return `
<section class="lib-cat" id="${cat.id}">
  <div class="lib-cat-head">
    <h2>${esc(cat.title)} <span class="lib-cat-count">${items.length}</span></h2>
    <p>${esc(cat.blurb)}</p>
  </div>
</section>
${items.map(componentBlock).join('\n')}`;
}

function build() {
  const pages = readPublishedPages();
  const total = Object.values(COMPONENTS).reduce((n, a) => n + a.length, 0);

  // group published pages by category for the index
  const byCat = {};
  for (const p of pages) (byCat[p.category] = byCat[p.category] || []).push(p);
  const pageIndex = Object.keys(byCat).sort().map(cat => `
      <div class="pi-group">
        <h6>${esc(cat)}</h6>
        ${byCat[cat].map(p => `<a href="${p.route}">${esc(p.label)}</a>`).join('')}
      </div>`).join('');

  const navLinks = CATEGORIES
    .filter(c => (COMPONENTS[c.id] || []).length)
    .map(c => `<a href="#${c.id}">${esc(c.title)}</a>`).join('\n    ');

  const sections = CATEGORIES
    .filter(c => (COMPONENTS[c.id] || []).length)
    .map(categorySection).join('\n');

  const html = `<!DOCTYPE html>
<html lang="en-GB">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
<title>BFC Component Library | Bespoke Foil Company</title>
<meta name="description" content="Internal component catalogue for the Bespoke Foil Company static site. Numbered, reusable building blocks locked to the BFC design system.">
<meta name="robots" content="noindex">
<!-- AUTO-GENERATED by scripts/generate-component-library.js from scripts/component-registry.js - do not hand-edit. -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link rel="preload" as="style" href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,340;0,9..144,420;0,9..144,560;1,9..144,340&family=Instrument+Sans:ital,wght@0,400;0,500;0,600;1,400&family=Nothing+You+Could+Do&display=swap" onload="this.onload=null;this.rel='stylesheet'">
<noscript><link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,340;0,9..144,420;0,9..144,560;1,9..144,340&family=Instrument+Sans:ital,wght@0,400;0,500;0,600;1,400&family=Nothing+You+Could+Do&display=swap" rel="stylesheet"></noscript>
<link rel="stylesheet" href="/shared/styles.css?v=10">
<style>
${CSS}
</style>
</head>
<body>

<header class="lib-mast">
  <div class="lib-mast-inner">
    <p class="kicker">Bespoke Foil Company - Internal</p>
    <h1>Component Library</h1>
    <p>Numbered, reusable building blocks locked to the BFC design system. Reference any block by its code - for example <code>H02</code>, <code>B04</code> or <code>CTA01</code> - and I'll build in that style.</p>
    <div class="lib-stats"><span><b>${total}</b> components</span><span><b>${CATEGORIES.filter(c=>(COMPONENTS[c.id]||[]).length).length}</b> categories</span><span><b>${pages.length}</b> pages published</span></div>
  </div>
</header>

<nav class="lib-nav">
  <div class="lib-nav-inner">
    ${navLinks}
    <a href="#published" class="pi-link">Published pages</a>
  </div>
</nav>

${sections}

<!-- ===================== PUBLISHED PAGES (auto) ===================== -->
<section class="lib-cat" id="published">
  <div class="lib-cat-head">
    <h2>Published pages <span class="lib-cat-count">${pages.length}</span></h2>
    <p>Live routes on the site right now. This list rebuilds automatically from <code>_redirects</code> each time the library is generated.</p>
  </div>
</section>
<div class="lib-item">
  <div class="page-index">${pageIndex}
  </div>
</div>

<div style="height:72px"></div>

<script>
/* FAQ accordion demo interactivity (previews only) */
document.querySelectorAll('.demo-faq .fa-item .fa-q').forEach(function(btn){
  btn.addEventListener('click',function(){
    var item=btn.closest('.fa-item');
    var open=item.classList.toggle('open');
    item.querySelector('.fa-ic').innerHTML = open ? '\\u2212' : '+';
  });
});
</script>
</body>
</html>`;

  fs.writeFileSync(OUT, html);
  console.log(`Component library generated: ${total} components across ${CATEGORIES.length} categories, ${pages.length} published pages indexed.`);
  for (const c of CATEGORIES) {
    const n = (COMPONENTS[c.id] || []).length;
    console.log(`  ${c.code.padEnd(5)} ${c.title.padEnd(20)} ${n}`);
  }
}

/* ---- catalogue chrome + preview helper CSS (single block) ---- */
const CSS = `
body{background:var(--porcelain)}
.lib-mast{background:var(--ink);color:var(--white);padding:64px 40px 52px}
.lib-mast-inner{max-width:1180px;margin:0 auto}
.lib-mast .kicker{font-family:var(--sans);font-size:12px;letter-spacing:.2em;text-transform:uppercase;color:var(--stone);margin-bottom:16px}
.lib-mast h1{font-family:var(--serif);font-weight:340;font-size:clamp(34px,5vw,52px);letter-spacing:-.02em;line-height:1.03;margin-bottom:16px}
.lib-mast p{font-family:var(--sans);font-size:16.5px;line-height:1.6;color:rgba(255,255,255,.72);max-width:640px}
.lib-mast code{font-family:ui-monospace,Menlo,monospace;font-size:.9em;background:rgba(255,255,255,.1);padding:2px 7px;border-radius:6px}
.lib-stats{display:flex;gap:28px;margin-top:26px}
.lib-stats span{font-family:var(--sans);font-size:14px;color:rgba(255,255,255,.6)}
.lib-stats b{font-family:var(--serif);font-size:22px;color:var(--white);margin-right:6px}

.lib-nav{position:sticky;top:0;z-index:50;background:rgba(246,246,244,.9);backdrop-filter:blur(10px);border-bottom:1px solid var(--hairline);padding:14px 40px}
.lib-nav-inner{max-width:1180px;margin:0 auto;display:flex;gap:8px;flex-wrap:wrap;align-items:center}
.lib-nav a{font-family:var(--sans);font-size:13px;font-weight:500;text-decoration:none;color:var(--ink-soft);padding:7px 14px;border-radius:999px;border:1px solid var(--hairline);transition:all .18s ease}
.lib-nav a:hover{background:var(--ink);color:var(--white);border-color:var(--ink)}
.lib-nav a.pi-link{margin-left:auto;background:var(--stone);border-color:var(--stone);color:var(--ink)}

.lib-cat{max-width:1180px;margin:0 auto;padding:60px 40px 4px}
.lib-cat-head{border-bottom:2px solid var(--ink);padding-bottom:14px}
.lib-cat-head h2{font-family:var(--serif);font-weight:420;font-size:clamp(24px,3.4vw,34px);letter-spacing:-.01em;display:flex;align-items:baseline;gap:12px}
.lib-cat-count{font-family:var(--sans);font-size:14px;font-weight:600;color:var(--white);background:var(--stone-text);border-radius:999px;padding:2px 11px}
.lib-cat-head p{font-family:var(--sans);font-size:15px;color:var(--ink-soft);margin-top:6px}

.lib-item{max-width:1180px;margin:0 auto;padding:30px 40px 4px}
.lib-item-bar{display:flex;align-items:baseline;justify-content:space-between;gap:16px;margin-bottom:14px;flex-wrap:wrap}
.lib-id{display:inline-flex;align-items:center;gap:10px}
.lib-ref{font-family:ui-monospace,Menlo,monospace;font-size:12px;font-weight:600;background:var(--ink);color:var(--white);padding:4px 10px;border-radius:6px}
.lib-item-bar h3{font-family:var(--serif);font-weight:420;font-size:19px;letter-spacing:-.01em}
.lib-desc{font-family:var(--sans);font-size:13.5px;color:var(--ink-soft);max-width:520px;text-align:right}
.lib-frame{border:1px solid var(--hairline);border-radius:var(--r-img);overflow:hidden;background:var(--white);box-shadow:0 12px 40px -28px rgba(0,0,0,.3)}
.lib-frame.is-dark{background:var(--ink)}
.lib-preview{position:relative;isolation:isolate}
.lib-preview header{position:absolute}

/* ---- HEADER demos ---- */
.demo-hero{position:relative;min-height:300px;display:flex;align-items:center;justify-content:center;overflow:hidden;background-size:cover;background-position:center}
.demo-hero.dark::after{content:"";position:absolute;inset:0;background:linear-gradient(180deg,rgba(0,0,0,.5),rgba(0,0,0,.25))}
.demo-hero.light{background:var(--porcelain)}
.demo-hero-copy{position:relative;z-index:2;text-align:center;padding:0 24px}
.demo-hero-copy .eyebrow{font-family:var(--sans);font-size:11px;letter-spacing:.18em;text-transform:uppercase;margin-bottom:12px}
.demo-hero-copy h4{font-family:var(--serif);font-weight:340;font-size:clamp(26px,4vw,40px);letter-spacing:-.02em;line-height:1.05}
.demo-hero.dark .demo-hero-copy{color:var(--white)}.demo-hero.dark .eyebrow{color:var(--stone)}
.demo-hero.light .demo-hero-copy{color:var(--ink)}.demo-hero.light .eyebrow{color:var(--stone-text)}
.mini-hd{position:absolute;top:0;left:0;right:0;z-index:5;display:flex;align-items:center;justify-content:space-between;padding:20px 26px}
.mini-burger{display:flex;flex-direction:column;gap:4px}
.mini-burger span{width:22px;height:2px;border-radius:2px;display:block}
.mini-logo{font-family:var(--serif);font-weight:500;font-size:19px;letter-spacing:-.01em}
.mini-right{display:flex;align-items:center;gap:14px}
.mini-pill{font-family:var(--sans);font-size:12.5px;font-weight:500;padding:8px 15px;border-radius:999px;border:1.5px solid}
.mini-cart svg{width:22px;height:22px;fill:none;stroke-width:1.6}
.mini-hd.on-dark .mini-burger span{background:#fff}
.mini-hd.on-dark .mini-logo,.mini-hd.on-dark .mini-cart svg{color:#fff;stroke:#fff}
.mini-hd.on-dark .mini-pill{color:#fff;border-color:rgba(255,255,255,.6)}
.mini-hd.on-light .mini-burger span{background:var(--ink)}
.mini-hd.on-light .mini-logo,.mini-hd.on-light .mini-cart svg{color:var(--ink);stroke:var(--ink)}
.mini-hd.on-light .mini-pill{color:var(--ink);border-color:var(--ink)}
.mini-hd.solid{background:var(--porcelain);box-shadow:0 2px 14px -8px rgba(0,0,0,.16);padding:16px 26px}
.mini-hd.centred{justify-content:center}
.mini-hd.centred .mini-side{position:absolute;top:50%;transform:translateY(-50%)}
.mini-hd.centred .mini-side.l{left:26px}.mini-hd.centred .mini-side.r{right:26px}

/* ---- GALLERY demos ---- */
.demo-scroll{display:flex;gap:16px;overflow-x:auto;padding:28px 26px;scroll-snap-type:x mandatory}
.demo-scroll::-webkit-scrollbar{height:6px}.demo-scroll::-webkit-scrollbar-thumb{background:var(--bone);border-radius:3px}
.demo-scroll .g-card{scroll-snap-align:start;flex:0 0 auto;width:230px;background:var(--porcelain);border-radius:var(--r-img);overflow:hidden;border:1px solid var(--hairline)}
.demo-scroll.peek .g-card{width:320px}
.g-ph{height:150px;background-size:cover;background-position:center}
.g-body{padding:14px 16px}
.g-body h5{font-family:var(--serif);font-weight:420;font-size:16px;margin-bottom:4px}
.g-body p{font-family:var(--sans);font-size:13px;color:var(--ink-soft)}
.demo-marquee{overflow:hidden;padding:30px 0;position:relative;background:var(--white)}
.demo-marquee::before,.demo-marquee::after{content:"";position:absolute;top:0;bottom:0;width:80px;z-index:2}
.demo-marquee::before{left:0;background:linear-gradient(90deg,var(--white),transparent)}
.demo-marquee::after{right:0;background:linear-gradient(270deg,var(--white),transparent)}
.demo-marquee-track{display:flex;gap:56px;width:max-content;animation:lib-marq 22s linear infinite}
.demo-marquee-track span{font-family:var(--serif);font-weight:420;font-size:22px;color:var(--stone-text);white-space:nowrap;opacity:.75}
@keyframes lib-marq{to{transform:translateX(-50%)}}
.demo-filmstrip{display:flex;flex-direction:column;gap:12px}
.fs-lead{aspect-ratio:16/9;border-radius:var(--r-img);background-size:cover;background-position:center}
.fs-thumbs{display:flex;gap:10px}
.fs-thumb{width:90px;height:64px;border-radius:10px;background-size:cover;background-position:center;opacity:.55;cursor:pointer;border:2px solid transparent}
.fs-thumb.is-active{opacity:1;border-color:var(--ink)}

/* ---- BODY demos ---- */
.demo-pad{padding:44px 40px}
.demo-split{display:grid;grid-template-columns:1fr 1fr;gap:36px;align-items:center}
.demo-split .d-media{aspect-ratio:4/3;border-radius:var(--r-img);background-size:cover;background-position:center}
.eyebrow{font-family:var(--sans);font-size:11px;letter-spacing:.16em;text-transform:uppercase;color:var(--stone-text);margin-bottom:12px}
.demo-split h4{font-family:var(--serif);font-weight:340;font-size:30px;letter-spacing:-.02em;line-height:1.1;margin-bottom:12px}
.demo-split p{font-family:var(--sans);font-size:15px;line-height:1.6;color:var(--ink-soft)}
.demo-centre{text-align:center;max-width:640px;margin:0 auto}
.demo-centre h4{font-family:var(--serif);font-weight:340;font-size:clamp(28px,4vw,40px);letter-spacing:-.02em;line-height:1.08;margin-bottom:14px}
.demo-centre h4 .script{font-family:var(--script);color:var(--stone-text);font-size:1.05em}
.demo-centre p{font-family:var(--sans);font-size:16px;line-height:1.62;color:var(--ink-soft)}
.demo-tiles{display:grid;grid-template-columns:repeat(3,1fr);gap:18px}
.d-tile{background:var(--white);border:1px solid var(--hairline);border-radius:var(--r-img);overflow:hidden;padding:0 0 22px}
.d-tile-img{height:130px;background-size:cover;background-position:center;margin-bottom:18px}
.d-tile h5{font-family:var(--serif);font-weight:420;font-size:18px;margin:0 22px 8px}
.d-tile p{font-family:var(--sans);font-size:14px;line-height:1.55;color:var(--ink-soft);margin:0 22px}
.demo-alt{display:flex;flex-direction:column;gap:24px}
.d-row{display:grid;grid-template-columns:1fr 1fr;gap:28px;align-items:center}
.d-row:nth-child(even) .d-media{order:2}
.d-media{aspect-ratio:16/10;border-radius:var(--r-img);background-size:cover;background-position:center}
.demo-alt h5{font-family:var(--serif);font-weight:420;font-size:21px;margin-bottom:8px;letter-spacing:-.01em}
.demo-alt p{font-family:var(--sans);font-size:14.5px;line-height:1.6;color:var(--ink-soft)}
.demo-stats{background:var(--ink);border-radius:var(--r-img);padding:40px;display:grid;grid-template-columns:repeat(3,1fr);gap:28px;text-align:center}
.d-stat .num{font-family:var(--serif);font-weight:340;font-size:44px;color:var(--white);letter-spacing:-.02em;line-height:1}
.d-stat .lbl{font-family:var(--sans);font-size:13px;letter-spacing:.06em;text-transform:uppercase;color:var(--stone);margin-top:10px}
.demo-bleed{min-height:260px;background-size:cover;background-position:center;display:flex;align-items:flex-end;position:relative}
.demo-bleed::after{content:"";position:absolute;inset:0;background:linear-gradient(180deg,transparent,rgba(0,0,0,.45))}
.demo-bleed-copy{position:relative;z-index:2;padding:34px 40px}
.demo-bleed-copy h4{font-family:var(--serif);font-weight:340;font-size:32px;color:var(--white);letter-spacing:-.02em}
.demo-steps{display:grid;grid-template-columns:repeat(4,1fr);gap:22px}
.d-step .s-n{font-family:var(--serif);font-weight:340;font-size:40px;color:var(--stone);line-height:1;margin-bottom:12px}
.d-step h5{font-family:var(--serif);font-weight:420;font-size:19px;margin-bottom:6px}
.d-step p{font-family:var(--sans);font-size:14px;color:var(--ink-soft);line-height:1.5}

/* ---- shared demo button ---- */
.demo-btn{display:inline-flex;align-items:center;gap:8px;font-family:var(--sans);font-size:14px;font-weight:500;padding:11px 18px;border-radius:var(--r-btn)}
.demo-btn svg{width:15px;height:15px;fill:none;stroke:currentColor;stroke-width:2}
.demo-btn.dark{background:var(--ink);color:var(--white)}
.demo-btn.light{background:var(--white);color:var(--ink)}
.demo-btn.ghost{background:transparent;color:var(--ink);box-shadow:inset 0 0 0 1.5px var(--ink)}

/* ---- CTA demos ---- */
.demo-ctaband{padding:56px 40px;text-align:center}
.demo-ctaband.dark{background:var(--ink);color:var(--white)}
.cb-inner{max-width:520px;margin:0 auto}
.cb-inner h4{font-family:var(--serif);font-weight:340;font-size:32px;letter-spacing:-.02em;margin-bottom:12px}
.cb-inner p{font-family:var(--sans);font-size:16px;color:rgba(255,255,255,.72);margin-bottom:24px}
.demo-ctasplit{display:grid;grid-template-columns:1fr 1fr;align-items:stretch;min-height:280px}
.cs-copy{padding:48px 44px;display:flex;flex-direction:column;justify-content:center}
.cs-copy h4{font-family:var(--serif);font-weight:340;font-size:30px;letter-spacing:-.02em;margin-bottom:12px}
.cs-copy p{font-family:var(--sans);font-size:15px;color:var(--ink-soft);margin-bottom:22px}
.cs-copy .demo-btn{align-self:flex-start}
.cs-media{background-size:cover;background-position:center}
.demo-ctastrip{display:flex;align-items:center;justify-content:space-between;gap:20px;background:var(--porcelain);border:1px solid var(--hairline);border-radius:var(--r-img);padding:20px 26px;flex-wrap:wrap}
.demo-ctastrip>span:first-child{font-family:var(--serif);font-size:19px}

/* ---- TESTIMONIAL demos ---- */
.demo-quotes{display:grid;grid-template-columns:repeat(3,1fr);gap:18px}
.q-card{background:var(--white);border:1px solid var(--hairline);border-radius:var(--r-img);padding:26px}
.q-stars{color:var(--stone);letter-spacing:2px;font-size:15px;margin-bottom:14px}
.q-stars.tp{color:#00B67A}
.q-text{font-family:var(--serif);font-size:17px;line-height:1.5;margin-bottom:20px}
.q-person{display:flex;align-items:center;gap:12px}
.q-av{width:40px;height:40px;border-radius:50%;background-size:cover;background-position:center}
.q-name{font-family:var(--sans);font-size:14px;font-weight:600}
.demo-featquote{display:grid;grid-template-columns:.8fr 1.2fr;gap:36px;align-items:center}
.fq-media{aspect-ratio:1;border-radius:var(--r-img);background-size:cover;background-position:center}
.fq-copy blockquote{font-family:var(--serif);font-weight:340;font-size:26px;line-height:1.35;letter-spacing:-.01em;margin:12px 0 16px}
.fq-copy cite{font-family:var(--sans);font-size:14px;color:var(--ink-soft);font-style:normal}
.demo-tpstrip{display:flex;align-items:center;justify-content:center;gap:14px;flex-wrap:wrap}
.tp-score{font-family:var(--serif);font-size:30px}
.tp-meta{font-family:var(--sans);font-size:14px;color:var(--ink-soft)}

/* ---- PRICING demos ---- */
.demo-pricing{display:grid;grid-template-columns:repeat(3,1fr);gap:18px;align-items:stretch}
.pr-card{position:relative;background:var(--white);border:1px solid var(--hairline);border-radius:var(--r-img);padding:30px 26px;text-align:center;display:flex;flex-direction:column}
.pr-card.is-feat{border-color:var(--ink);box-shadow:0 18px 44px -24px rgba(0,0,0,.3)}
.pr-tag{position:absolute;top:-11px;left:50%;transform:translateX(-50%);background:var(--ink);color:var(--white);font-family:var(--sans);font-size:11px;letter-spacing:.08em;text-transform:uppercase;padding:4px 12px;border-radius:999px}
.pr-card h5{font-family:var(--serif);font-weight:420;font-size:19px;margin-bottom:6px}
.pr-price{font-family:var(--serif);font-weight:340;font-size:40px;letter-spacing:-.02em;margin-bottom:10px}
.pr-card p{font-family:var(--sans);font-size:14px;color:var(--ink-soft);margin-bottom:22px;flex:1}
.pr-card .demo-btn{justify-content:center}
.demo-cmp{width:100%;border-collapse:collapse;font-family:var(--sans)}
.demo-cmp th,.demo-cmp td{padding:14px 16px;text-align:center;border-bottom:1px solid var(--hairline)}
.demo-cmp thead th{font-family:var(--serif);font-weight:420;font-size:17px}
.demo-cmp td:first-child,.demo-cmp th:first-child{text-align:left;color:var(--ink-soft);font-size:14px}
.demo-cmp tbody td{color:var(--stone-text);font-size:16px}

/* ---- FAQ demos ---- */
.demo-faq{max-width:720px;margin:0 auto}
.fa-item{border-bottom:1px solid var(--hairline)}
.fa-q{width:100%;background:none;border:none;text-align:left;font-family:var(--serif);font-size:18px;padding:20px 0;display:flex;justify-content:space-between;align-items:center;gap:16px;color:var(--ink)}
.fa-ic{font-family:var(--sans);font-size:22px;color:var(--stone-text);flex:0 0 auto}
.fa-a{font-family:var(--sans);font-size:15px;line-height:1.6;color:var(--ink-soft);max-height:0;overflow:hidden;transition:max-height .3s ease,padding .3s ease}
.fa-item.open .fa-a{max-height:200px;padding-bottom:20px}
.demo-faqgrid{display:grid;grid-template-columns:1fr 1fr;gap:24px 40px}
.fg-item h5{font-family:var(--serif);font-weight:420;font-size:17px;margin-bottom:6px}
.fg-item p{font-family:var(--sans);font-size:14.5px;color:var(--ink-soft);line-height:1.55}

/* ---- FORM demos ---- */
.demo-emailcap{background:var(--stone);border-radius:var(--r-img);padding:36px 40px;display:flex;align-items:center;justify-content:space-between;gap:28px;flex-wrap:wrap}
.demo-emailcap h4{font-family:var(--serif);font-weight:340;font-size:26px;margin-bottom:6px}
.demo-emailcap p{font-family:var(--sans);font-size:14px;color:var(--ink-soft)}
.ec-form{display:flex;gap:10px}
.ec-form input{border:1px solid var(--ink);border-radius:var(--r-btn);padding:12px 15px;font-family:var(--sans);font-size:14px;background:var(--white);min-width:230px}
.demo-enquiry{max-width:520px;margin:0 auto;background:var(--white);border:1px solid var(--hairline);border-radius:var(--r-img);padding:36px}
.demo-enquiry h4{font-family:var(--serif);font-weight:340;font-size:26px;letter-spacing:-.02em;margin-bottom:22px}
.eq-field{margin-bottom:16px}
.eq-field label{display:block;font-family:var(--sans);font-size:12.5px;font-weight:600;margin-bottom:7px}
.eq-field input,.eq-field textarea{width:100%;border:1px solid var(--hairline);border-radius:var(--r-btn);padding:12px 14px;font-family:var(--sans);font-size:14px;background:var(--porcelain)}
.eq-field textarea{min-height:90px;resize:vertical}

/* ---- FOOTER demos ---- */
.demo-footer{background:var(--ink);color:var(--white);padding:44px 40px 28px}
.df-cols{display:grid;grid-template-columns:repeat(4,1fr);gap:28px;margin-bottom:28px}
.df-col h6{font-family:var(--sans);font-size:12px;letter-spacing:.1em;text-transform:uppercase;color:var(--stone);margin-bottom:14px}
.df-col span{display:block;font-family:var(--sans);font-size:13.5px;color:rgba(255,255,255,.66);margin-bottom:8px}
.df-legal{border-top:1px solid rgba(255,255,255,.14);padding-top:20px;display:flex;justify-content:space-between;font-family:var(--sans);font-size:13px;color:rgba(255,255,255,.55);flex-wrap:wrap;gap:10px}
.demo-footer.slim{display:flex;align-items:center;justify-content:space-between;padding:26px 40px;flex-wrap:wrap;gap:16px}
.dfs-logo{font-family:var(--serif);font-weight:500;font-size:18px}
.dfs-links{display:flex;gap:20px}
.dfs-links span{font-family:var(--sans);font-size:14px;color:rgba(255,255,255,.7)}
.dfs-legal{font-family:var(--sans);font-size:12.5px;color:rgba(255,255,255,.5)}

/* ---- published-pages index ---- */
.page-index{display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:24px 32px}
.pi-group h6{font-family:var(--sans);font-size:12px;letter-spacing:.1em;text-transform:uppercase;color:var(--stone-text);margin-bottom:12px;border-bottom:1px solid var(--hairline);padding-bottom:8px}
.pi-group a{display:block;font-family:var(--sans);font-size:14px;color:var(--ink);text-decoration:none;padding:5px 0;transition:color .15s}
.pi-group a:hover{color:var(--stone-text)}

@media(max-width:760px){
  .lib-mast,.lib-nav,.lib-cat,.lib-item{padding-left:20px;padding-right:20px}
  .lib-desc{text-align:left}
  .lib-stats{gap:18px;flex-wrap:wrap}
  .demo-split,.d-row,.demo-featquote,.demo-ctasplit{grid-template-columns:1fr}
  .d-row:nth-child(even) .d-media{order:0}
  .demo-tiles,.demo-quotes,.demo-pricing,.demo-steps,.demo-faqgrid,.df-cols{grid-template-columns:1fr}
  .demo-stats{grid-template-columns:1fr;gap:24px}
  .cs-media{min-height:200px}
  .demo-emailcap{flex-direction:column;align-items:stretch}
  .ec-form input{min-width:0;flex:1}
}
`;

build();
